# Directed Reading Questions: Virtual Environments and Dependencies

Complete the following short questions after reading [virtual_environments_dependencies.md](virtual_environments_dependencies.md).

## Question 1: Global Python Packages

Why can installing a third-party package into the system-wide Python installation cause problems for other projects or operating system tools?

## Question 2: Virtual Environment Isolation

What does a virtual environment give each Python project its own copy of?

## Question 3: Managing a Project with `uv`

Which `uv` command installs the dependencies recorded in `pyproject.toml` and `uv.lock` when you clone a project?